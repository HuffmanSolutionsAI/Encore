import Foundation

/// Central place for product strings. "Encore" is a working name pending
/// trademark clearance (build spec Section 12) — keep the name changeable here.
enum AppConfig {
    static let appName = "Encore"
    static let brandLine = "Music worth playing again."
    static let ratingPrompt = "Worth an encore?"
}
