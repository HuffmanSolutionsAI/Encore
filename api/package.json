{
  "name": "encore-api",
  "version": "1.0.0",
  "private": true,
  "description": "Encore backend API — AWS Lambda behind API Gateway.",
  "license": "UNLICENSED",
  "scripts": {
    "build": "esbuild src/handler.ts --bundle --platform=node --target=node20 --outfile=dist/handler.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^20.17.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  }
}
